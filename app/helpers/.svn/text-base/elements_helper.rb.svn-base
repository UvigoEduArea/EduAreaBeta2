module ElementsHelper
  def ext_link(url)
    return_data = ""
    unless url.nil?
      unless url.include?("http://")
        return_data = url.insert(0, "http://")
      end
      p url
    end
    return return_data  
  end
    
  def self.ext_link(url)
    return_data = ""
    unless url.nil?
      unless url.include?("http://")
        return_data = url.insert(0, "http://")
      end
      p url
    end
    return return_data
  end
  
end
